version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile    
    ports:
    - "8081:8081"
    restart: always
    command:
      bash -c "ng serve --host 0.0.0.0 --port 8080 --disable-host-check"
    